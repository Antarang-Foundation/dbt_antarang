version: 2
models:
  - name: fct_attendance_assessment
    # description:
    columns:
      - name: student_barcode
        description: "The primary key of this table"
        tests:
          - unique
          - not_null
  - name: fct_attendance_assessment_cdm1_cdm2
    # description:
    columns:
      - name: student_barcode
        description: "The primary key of this table"
        tests:
          - unique

  - name: fct_hm_assessment
    # description:
    columns:
      - name: school_name
        description: "The primary key of this table"
