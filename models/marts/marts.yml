version : 2
models:
  - name: fct_attendance_assessment
    # description:
    columns:
      - name: student_barcode
        description: "The primary key of this table"
        tests:
          - unique
          - not_null
  - name: fct_attendance_assessment_cdm1_cdm2
    # description:
    columns:
      - name: student_barcode
        description: "The primary key of this table"
        tests:
          - unique
  - name: fct_student_grade_performance
    # description:
    columns:
      - name: contact_id
        description: "The primary key of this table"
        tests:
          - not_null
    columns:
      - name: barcode
        description: "The second primary key of this table"
        tests:
          - not_null